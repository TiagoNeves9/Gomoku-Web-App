docker-compose build --no-cache
    (construir os containers)

docker-compose up -d
    (iniciar os containers -d inicia em 2plano)

docker-compose ps
    (verifica se os containers estão em execução e se n há erros)


docker-compose logs -f
    (logs em tempo real)

docker-compose down
    (parar e remover os containers)

